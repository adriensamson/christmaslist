{% extends 'KyklydseChristmasListBundle::layout.html.twig' %}

{% block content %}

{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_rest(form) }}

    <button type="submit">{% trans %}Validate{% endtrans %}</button>
{{ form_end(form) }}
<a class="button" href="{{ path('kyklydse_christmaslist_list_view', {'id': list.id}) }}">{{ 'Back to list' | trans }}</a>

{% endblock %}
